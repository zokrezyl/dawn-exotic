name: build-aarch64

on:
  push:
    tags: ['*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify tag matches dawn-version
        id: ver
        run: |
          set -euo pipefail
          tag="${GITHUB_REF_NAME}"
          version="$(tr -d '[:space:]' < dawn-version)"
          tag_stripped="${tag#v}"
          if [[ "${tag_stripped}" != "${version}" ]]; then
            echo "ERROR: tag '${tag}' does not match dawn-version '${version}'" >&2
            echo "       (compared as '${tag_stripped}' vs '${version}')" >&2
            exit 1
          fi
          echo "tag '${tag}' matches dawn-version '${version}'"
          echo "dawn_version=${version}" >> "$GITHUB_OUTPUT"

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            cmake ninja-build python3 git ca-certificates pkg-config \
            libwayland-dev wayland-protocols libwayland-bin \
            libxkbcommon-dev libx11-dev libx11-xcb-dev libxrandr-dev libxinerama-dev \
            libxcursor-dev libxi-dev libxext-dev libxcb1-dev \
            libvulkan-dev mesa-common-dev

      - name: Cache depot_tools + Dawn source
        uses: actions/cache@v4
        with:
          path: |
            .cache/depot_tools
            .cache/dawn-${{ steps.ver.outputs.dawn_version }}
          key: dawn-aarch64-${{ steps.ver.outputs.dawn_version }}

      - name: Build (Debug + Release)
        run: ./build-tools/aarch64/build.sh

      - name: Package release tarballs (cmake --install + tar)
        id: pkg
        run: |
          set -euo pipefail
          version="${{ steps.ver.outputs.dawn_version }}"
          mkdir -p release
          for bt in debug release; do
              stage="dawn-linux-aarch64-${bt}-${version}"
              cmake --install "build-aarch64-${bt}" --prefix "release/${stage}"
              tar -C release -czf "release/${stage}.tar.gz" "${stage}"
              rm -rf "release/${stage}"
          done
          ls -la release/

      - name: Upload workflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: libwebgpu_dawn-aarch64-${{ steps.ver.outputs.dawn_version }}
          path: release/*.tar.gz
          if-no-files-found: error

      - name: Attach to GitHub release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: release/*.tar.gz
          fail_on_unmatched_files: true
